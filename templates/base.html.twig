<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Gestion Meubles{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <style>
        body { background-color: #f1f3f6; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
        .navbar { background-color: #3498db !important; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        .navbar a.navbar-brand { color: #fff; font-weight: 700; font-size: 1.5rem; }
        .navbar a.btn { margin-left: 5px; }
        .container { background-color: #ffffff; padding: 40px; border-radius: 12px; margin-top: 40px; }
        h1 { color: #2c3e50; font-weight: 700; border-left: 5px solid #3498db; padding-left: 12px; margin-bottom: 30px; }
    </style>

    {% block stylesheets %}{% endblock %}
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">Gestion des Meubles</a>
        <div class="d-flex">
            {% if app.user %}
                {# Affiche les liens Meubles et Catégories uniquement après login #}
                <a class="btn btn-success" href="{{ path('app_category') }}">Catégories</a>
                <a class="btn btn-success" href="{{ path('app_meubles') }}">Meubles</a>
                <a class="btn btn-danger" href="{{ path('app_logout') }}">Déconnexion</a>
            {% else %}
                <a class="btn btn-primary" href="{{ path('app_login') }}">Connexion</a>
            {% endif %}
        </div>
    </div>
</nav>

<div class="container">
    {% block body %}{% endblock %}
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
{% block javascripts %}{% endblock %}
</body>
</html>
