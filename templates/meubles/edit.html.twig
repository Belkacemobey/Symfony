{% extends 'base.html.twig' %}

{% block title %}Modifier Meuble{% endblock %}

{% block body %}
    <h1>Modifier le meuble</h1>

    {{ form_start(form) }}
        <div class="row">
            <div class="col-md-6">
                {{ form_row(form.nomM) }}
                {{ form_row(form.prix) }}
                {{ form_row(form.since_At) }}
            </div>
            <div class="col-md-6">
                {{ form_row(form.category) }}
                {{ form_row(form.imageFile) }}
                {{ form_row(form.description) }}
            </div>
        </div>
        
        <button class="btn btn-success">Enregistrer</button>
        <a href="{{ path('app_meubles_index') }}" class="btn btn-secondary">Retour à la liste</a>
    {{ form_end(form) }}
{% endblock %}